stringArr = [firstValue, symbol ,secondValue]

const calculateResult = () => {
    if stringArr[1]= subtract; {
     const finalResult = (Number(stringArr[0])-(Number(stringArr[2])));
     alert (finalResult)
    }
}




form.addEventListener()
form.addEventListener("submit", calculateResult); 