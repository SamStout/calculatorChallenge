stringArr = [firstValue, symbol ,secondValue]

const calculateResult =() => {
    if (symbol.value="subtract") {
     const finalResult = (Number(firstValue.value))-(Number(secondValue.value))
     alert (finalResult)
    }
}




form.addEventListener()
form.addEventListener("submit", calculateResult); 